# Web framework
flask>=3.1
flask-sqlalchemy>=3.1
flask-migrate>=4.1
flask-login>=0.6.3
flask-admin>=2.0

# Database
sqlalchemy>=2.0
psycopg[binary]>=3.2

# Config, validation, secrets
python-dotenv>=1.0
email-validator>=2.2
cryptography>=45

# Markdown rendering — admin panel, and the PDF export
markdown>=3.7

# PDF export (pure-Python, no system libraries)
fpdf2>=2.8.2

# LLM providers
openai>=2.0
anthropic>=0.60
google-genai>=1.0

# Tests
pytest>=8.3
